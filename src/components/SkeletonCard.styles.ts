import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    borderRadius: 16,
    padding: 16,
    marginBottom: 12,
    flexDirection: 'row',
    alignItems: 'center',
  },
  logoPlaceholder: {
    width: 48,
    height: 48,
    borderRadius: 24,
  },
  textBlock: {
    flex: 1,
    marginLeft: 12,
  },
  line: {
    height: 10,
    borderRadius: 4,
    marginBottom: 8,
  },
  chip: {
    height: 16,
    borderRadius: 8,
    width: '30%',
  },
});

